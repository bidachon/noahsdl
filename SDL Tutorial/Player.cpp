#include <stdio.h>
#include "Player.h"
#include <string>

void Player::load(int x, int y, int width, int height, std::string textureID)
{
    Player::load(x, y, width, height, textureID);
}

void Player::draw(SDL_Renderer* renderer)
{
    Player::draw(renderer);
}

void Player::update()
{
    
}

